<div class="container mt-5" data-controller="whatsapp-manager">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <!-- Service Management Panel -->
      <div class="card mb-4">
        <div class="card-header">
          <h5 class="mb-0">🔧 Gerenciamento do Serviço WhatsApp</h5>
        </div>
        <div class="card-body">
          <div class="row align-items-center">
            <div class="col-md-8">
              <div id="service-status">
                <span data-whatsapp-manager-target="statusText">🔄 Verificando status...</span>
              </div>
            </div>
            <div class="col-md-4 text-end">
              <div class="btn-group" role="group">
                <button type="button" 
                        data-action="click->whatsapp-manager#startService"
                        class="btn btn-success btn-sm">
                  ▶️ Iniciar
                </button>
                <button type="button" 
                        data-action="click->whatsapp-manager#restartService"
                        class="btn btn-warning btn-sm">
                  🔄 Reiniciar
                </button>
                <button type="button" 
                        data-action="click->whatsapp-manager#stopService"
                        class="btn btn-danger btn-sm">
                  ⏹️ Parar
                </button>
              </div>
            </div>
          </div>
          <div class="mt-2">
            <small class="text-muted">Status do serviço será atualizado automaticamente.</small>
          </div>
        </div>
      </div>

      <!-- WhatsApp Authentication Panel -->
      <div class="card">
        <div class="card-header">
          <h3 class="text-center">🔗 Autenticação do WhatsApp</h3>
        </div>
        <div class="card-body text-center">
          <!-- Messages -->
          <div data-whatsapp-manager-target="messages"></div>
          
          <!-- Loading State -->
          <div data-whatsapp-manager-target="loadingState">
            <div class="spinner-border text-primary mb-3" role="status">
              <span class="visually-hidden">Carregando...</span>
            </div>
            <p>Inicializando WhatsApp...</p>
          </div>

          <!-- QR Code Container -->
          <div data-whatsapp-manager-target="qrContainer" style="display: none;">
            <div class="alert alert-info">
              <i class="fas fa-mobile-alt"></i>
              <strong>Escaneie o QR Code com seu WhatsApp</strong><br>
              <small>Abra o WhatsApp → Menu (⋮) → Dispositivos conectados → Conectar um dispositivo</small>
            </div>
            <div data-whatsapp-manager-target="qrCode" class="d-flex justify-content-center mb-3"></div>
            <button data-action="click->whatsapp-manager#refreshQRCode" 
                    class="btn btn-outline-primary btn-sm">
              🔄 Gerar novo QR Code
            </button>
          </div>

          <!-- Success State -->
          <div data-whatsapp-manager-target="successState" style="display: none;">
            <div class="text-success mb-3">
              <i class="fas fa-check-circle" style="font-size: 48px;"></i>
            </div>
            <h4 class="text-success">WhatsApp Conectado com Sucesso!</h4>
            <p class="text-muted">Você já pode usar o sistema. Esta página pode ser fechada.</p>
          </div>

          <!-- Error State -->
          <div data-whatsapp-manager-target="errorState" style="display: none;">
            <div class="text-danger mb-3">
              <i class="fas fa-exclamation-triangle" style="font-size: 48px;"></i>
            </div>
            <h4 class="text-danger">Erro na Conexão</h4>
            <p data-whatsapp-manager-target="errorMessage" class="text-muted"></p>
            <button data-action="click->whatsapp-manager#restart" 
                    class="btn btn-primary">
              🔄 Tentar Novamente
            </button>
          </div>

          <!-- Debug Info -->
          <div class="mt-4">
            <details>
              <summary class="btn btn-link btn-sm">🔍 Informações de Debug</summary>
              <div class="mt-2">
                <pre data-whatsapp-manager-target="debugInfo" 
                     class="text-start bg-light p-2 rounded" 
                     style="font-size: 0.8rem;"></pre>
              </div>
            </details>
          </div>
        </div>
      </div>
    </div>
  </div>
</div> 