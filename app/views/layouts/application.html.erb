<!DOCTYPE html>
<html class="h-full">
<head>
  <title>BillBuddy</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= action_cable_meta_tag %>
  
  <!-- Favicon -->
  <%= favicon_link_tag "/icon.png", type: "image/png" %>
  <%= favicon_link_tag "/icon.svg", type: "image/svg+xml" %>
  <link rel="apple-touch-icon" href="/icon.png">
  
  <!-- Stylesheet -->
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  
  <!-- Tailwind CSS via CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      darkMode: 'class'
    }
  </script>

  <!-- Font Awesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">

  <!-- FullCalendar CSS -->
  <link href="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.18/main.min.css" rel="stylesheet">
  
  <!-- FullCalendar JS -->
  <script src="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.18/index.global.min.js"></script>

  <!-- JavaScript - Load AFTER CSS -->
  <%= javascript_importmap_tags %>
  <script>
    // Force reload of WhatsApp controllers to clear cache
    if (window.performance && window.performance.navigation.type === 1) {
      console.log('🔄 Page reloaded - WhatsApp controllers will be refreshed');
    }
  </script>

  <style>
      /* Componentes personalizados */
      .btn {
          @apply px-4 py-2 rounded font-medium transition-colors duration-200;
      }

      .btn-primary {
          @apply bg-indigo-600 text-white hover:bg-indigo-700 dark:bg-purple-600 dark:hover:bg-purple-700;
      }

      .btn-secondary {
          @apply bg-gray-500 text-white hover:bg-gray-600 dark:bg-gray-800 dark:hover:bg-gray-700;
      }

      .btn-danger {
          @apply bg-red-600 text-white hover:bg-red-700 dark:bg-red-600 dark:hover:bg-red-700;
      }

      .card {
          @apply bg-white rounded-lg shadow-md overflow-hidden;
      }
      
      .dark .card {
          background-color: #1a1a1a;
      }

      .card-header {
          @apply px-6 py-4 border-b border-gray-200;
      }
      
      .dark .card-header {
          border-color: #333333;
      }

      .card-body {
          @apply p-6;
      }

      .form-input {
          @apply w-full px-3 py-2 border border-gray-300 rounded-md
          focus:outline-none focus:ring-2 focus:ring-indigo-500
          bg-white text-gray-900;
      }
      
      .dark .form-input {
          background-color: #2a2a2a;
          border-color: #444444;
          color: #f5f5f5;
      }
      
      .dark .form-input:focus {
          ring-color: #8b5cf6;
      }

      .form-label {
          @apply block text-sm font-medium text-gray-700 mb-1;
      }
      
      .dark .form-label {
          color: #d1d5db;
      }

      .table-container {
          @apply overflow-x-auto rounded-lg shadow;
      }

      .table {
          @apply min-w-full divide-y divide-gray-200;
      }
      
      .dark .table {
          divide-color: #333333;
      }

      .table-header {
          @apply bg-gray-50;
      }
      
      .dark .table-header {
          background-color: #2a2a2a;
      }

      .table-header-cell {
          @apply px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider;
      }
      
      .dark .table-header-cell {
          color: #9ca3af;
      }

      .table-body {
          @apply bg-white divide-y divide-gray-200;
      }
      
      .dark .table-body {
          background-color: #1a1a1a;
          divide-color: #333333;
      }

      .table-row {
          @apply hover:bg-gray-50;
      }
      
      .dark .table-row:hover {
          background-color: #2a2a2a;
      }

      .table-cell {
          @apply px-6 py-4 whitespace-nowrap text-sm text-gray-900;
      }
      
      .dark .table-cell {
          color: #f5f5f5;
      }

      /* Fix for scrolling background */
      body {
          background-color: #f9fafb; /* gray-50 */
      }
      
      .dark body {
          background-color: #0f0f0f; /* Very dark gray, almost black */
      }
      
      /* Dark theme overrides for white components */
      .dark .bg-white {
          background-color: #1a1a1a !important;
      }
      
      .dark .bg-gray-50 {
          background-color: #2a2a2a !important;
      }
      
      .dark .bg-gray-100 {
          background-color: #2a2a2a !important;
      }
      
      .dark .text-gray-900 {
          color: #f5f5f5 !important;
      }
      
      .dark .text-gray-600 {
          color: #d1d5db !important;
      }
      
      .dark .text-gray-500 {
          color: #9ca3af !important;
      }
      
      .dark .border-gray-200 {
          border-color: #333333 !important;
      }
      
      .dark .border-gray-300 {
          border-color: #444444 !important;
      }
  </style>
</head>

<body class="h-full text-gray-900 dark:text-gray-100" data-controller="theme">
<div class="min-h-screen bg-gray-50 dark:bg-gray-950">
  <!-- Only show navbar if not on authentication pages -->
  <% unless devise_controller? %>
  <!-- Navbar -->
  <nav class="bg-gradient-to-r from-slate-800 via-slate-900 to-slate-800 dark:from-slate-900 dark:via-purple-900 dark:to-slate-900 shadow-lg border-b border-slate-700 dark:border-purple-800">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16">
        <div class="flex">
          <div class="flex-shrink-0 flex items-center">
            <h1 class="text-xl font-bold bg-gradient-to-r from-purple-400 to-purple-600 dark:from-purple-300 dark:to-purple-500 bg-clip-text text-transparent">BillBuddy</h1>
          </div>
          <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
            <%= link_to "Dashboard", dashboard_path, class: "#{current_page?(dashboard_path) ? 'border-purple-400 dark:border-purple-300 text-white dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:border-purple-300 dark:hover:border-purple-400 hover:text-white dark:hover:text-purple-200'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium transition-colors duration-200" %>

            <%= link_to "Clientes", customers_path, class: "#{controller_name == 'customers' ? 'border-purple-400 dark:border-purple-300 text-white dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:border-purple-300 dark:hover:border-purple-400 hover:text-white dark:hover:text-purple-200'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium transition-colors duration-200" %>

            <%= link_to "Compromissos", appointments_path, class: "#{controller_name == 'appointments' ? 'border-purple-400 dark:border-purple-300 text-white dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:border-purple-300 dark:hover:border-purple-400 hover:text-white dark:hover:text-purple-200'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium transition-colors duration-200" %>

            <%= link_to "Agenda", calendars_path, class: "#{controller_name == 'calendars' ? 'border-purple-400 dark:border-purple-300 text-white dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:border-purple-300 dark:hover:border-purple-400 hover:text-white dark:hover:text-purple-200'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium transition-colors duration-200" %>

            <%= link_to "Daily Classes", daily_completion_calendars_path, class: "#{action_name == 'daily_completion' ? 'border-purple-400 dark:border-purple-300 text-white dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:border-purple-300 dark:hover:border-purple-400 hover:text-white dark:hover:text-purple-200'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium transition-colors duration-200" %>

            <%= link_to "Métricas", metrics_calendars_path, class: "#{action_name == 'metrics' ? 'border-purple-400 dark:border-purple-300 text-white dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:border-purple-300 dark:hover:border-purple-400 hover:text-white dark:hover:text-purple-200'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium transition-colors duration-200" %>

            <%= link_to "Payments", monthly_checklist_payments_path, class: "#{controller_name == 'payments' && action_name == 'monthly_checklist' ? 'border-purple-400 dark:border-purple-300 text-white dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:border-purple-300 dark:hover:border-purple-400 hover:text-white dark:hover:text-purple-200'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium transition-colors duration-200" %>

            <%= link_to "WhatsApp", whatsapp_auth_path, class: "#{controller_name == 'whatsapp' ? 'border-purple-400 dark:border-purple-300 text-white dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:border-purple-300 dark:hover:border-purple-400 hover:text-white dark:hover:text-purple-200'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium transition-colors duration-200" %>
          </div>
        </div>
        <div class="flex items-center space-x-2">
          <!-- WhatsApp Status Indicator -->
          <div data-controller="whatsapp-status" class="relative">
            <div data-whatsapp-status-target="indicator" 
                 class="flex items-center space-x-1 px-2 py-1 rounded-full text-xs transition-all duration-200 cursor-pointer"
                 data-action="click->whatsapp-status#toggleTooltip"
                 title="Status do WhatsApp">
              <div data-whatsapp-status-target="dot" class="w-2 h-2 rounded-full bg-gray-400"></div>
              <i class="fab fa-whatsapp text-sm"></i>
            </div>
            
            <!-- Tooltip -->
            <div data-whatsapp-status-target="tooltip" 
                 class="hidden absolute right-0 mt-2 w-48 bg-gray-900 dark:bg-gray-800 text-white text-xs rounded-lg p-2 shadow-lg z-50">
              <div data-whatsapp-status-target="tooltipText">Verificando status...</div>
              <div class="absolute -top-1 right-4 w-2 h-2 bg-gray-900 dark:bg-gray-800 transform rotate-45"></div>
            </div>
          </div>

          <button data-action="theme#toggle" class="p-2 rounded-full text-slate-300 dark:text-slate-400 hover:bg-slate-700 dark:hover:bg-purple-800 hover:text-white dark:hover:text-purple-200 transition-colors duration-200">
            <i id="theme-icon" class="fas fa-moon"></i>
          </button>

          <div class="ml-3 relative" data-controller="dropdown">
            <div>
              <button type="button" 
                      data-action="click->dropdown#toggle" 
                      class="max-w-xs bg-slate-700 dark:bg-purple-800 rounded-full flex items-center text-sm focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-purple-400 dark:focus:ring-purple-300 ring-offset-slate-800 dark:ring-offset-purple-900 transition-all duration-200" 
                      id="user-menu-button" 
                      aria-expanded="false" 
                      aria-haspopup="true">
                <span class="sr-only">Abrir menu do usuário</span>
                <span class="h-8 w-8 rounded-full bg-purple-600 dark:bg-purple-700 flex items-center justify-center border border-purple-500 dark:border-purple-600">
                    <span class="text-white dark:text-purple-100 font-medium text-sm"><%= current_user&.email&.first&.upcase || 'U' %></span>
                  </span>
              </button>
            </div>

            <!-- Dropdown menu -->
            <div class="hidden absolute right-0 mt-2 w-56 rounded-md shadow-lg bg-white dark:bg-gray-800 ring-1 ring-black ring-opacity-5 focus:outline-none z-50" 
                 data-dropdown-target="menu" 
                 role="menu" 
                 aria-orientation="vertical" 
                 aria-labelledby="user-menu-button">
              <div class="py-1" role="none">
                <!-- User info -->
                <div class="px-4 py-3 border-b border-gray-200 dark:border-gray-600">
                  <p class="text-sm text-gray-900 dark:text-gray-100 font-medium">
                    <%= current_user&.email || 'Usuário' %>
                  </p>
                  <p class="text-xs text-gray-500 dark:text-gray-400">
                    Conectado
                  </p>
                </div>
                
                <!-- Profile link -->
                <%= link_to profile_path, 
                    class: "flex items-center px-4 py-2 text-sm text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors duration-200",
                    role: "menuitem" do %>
                  <i class="fas fa-user-circle mr-3 text-gray-400"></i>
                  Meu Perfil
                <% end %>
                
                <!-- Logout link -->
                <%= button_to destroy_user_session_path, 
                    method: :delete,
                    class: "w-full text-left flex items-center px-4 py-2 text-sm text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors duration-200",
                    role: "menuitem" do %>
                  <i class="fas fa-sign-out-alt mr-3 text-gray-400"></i>
                  Sair
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Mobile menu -->
    <div class="sm:hidden" id="mobile-menu">
      <div class="pt-2 pb-3 space-y-1 bg-slate-800 dark:bg-slate-900 border-t border-slate-700 dark:border-purple-800">
        <%= link_to "Dashboard", dashboard_path, class: "#{current_page?(dashboard_path) ? 'bg-purple-900 dark:bg-purple-800 border-purple-400 dark:border-purple-300 text-purple-100 dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:bg-slate-700 dark:hover:bg-purple-900 hover:border-purple-400 dark:hover:border-purple-300 hover:text-white dark:hover:text-purple-200'} block pl-3 pr-4 py-2 border-l-4 text-base font-medium transition-colors duration-200" %>

        <%= link_to "Clientes", customers_path, class: "#{controller_name == 'customers' ? 'bg-purple-900 dark:bg-purple-800 border-purple-400 dark:border-purple-300 text-purple-100 dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:bg-slate-700 dark:hover:bg-purple-900 hover:border-purple-400 dark:hover:border-purple-300 hover:text-white dark:hover:text-purple-200'} block pl-3 pr-4 py-2 border-l-4 text-base font-medium transition-colors duration-200" %>

        <%= link_to "Compromissos", appointments_path, class: "#{controller_name == 'appointments' ? 'bg-purple-900 dark:bg-purple-800 border-purple-400 dark:border-purple-300 text-purple-100 dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:bg-slate-700 dark:hover:bg-purple-900 hover:border-purple-400 dark:hover:border-purple-300 hover:text-white dark:hover:text-purple-200'} block pl-3 pr-4 py-2 border-l-4 text-base font-medium transition-colors duration-200" %>

        <%= link_to "Agenda", calendars_path, class: "#{controller_name == 'calendars' ? 'bg-purple-900 dark:bg-purple-800 border-purple-400 dark:border-purple-300 text-purple-100 dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:bg-slate-700 dark:hover:bg-purple-900 hover:border-purple-400 dark:hover:border-purple-300 hover:text-white dark:hover:text-purple-200'} block pl-3 pr-4 py-2 border-l-4 text-base font-medium transition-colors duration-200" %>

        <%= link_to "Daily Classes", daily_completion_calendars_path, class: "#{action_name == 'daily_completion' ? 'bg-purple-900 dark:bg-purple-800 border-purple-400 dark:border-purple-300 text-purple-100 dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:bg-slate-700 dark:hover:bg-purple-900 hover:border-purple-400 dark:hover:border-purple-300 hover:text-white dark:hover:text-purple-200'} block pl-3 pr-4 py-2 border-l-4 text-base font-medium transition-colors duration-200" %>

        <%= link_to "Métricas", metrics_calendars_path, class: "#{action_name == 'metrics' ? 'bg-purple-900 dark:bg-purple-800 border-purple-400 dark:border-purple-300 text-purple-100 dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:bg-slate-700 dark:hover:bg-purple-900 hover:border-purple-400 dark:hover:border-purple-300 hover:text-white dark:hover:text-purple-200'} block pl-3 pr-4 py-2 border-l-4 text-base font-medium transition-colors duration-200" %>

        <%= link_to "Checklist", monthly_checklist_payments_path, class: "#{controller_name == 'payments' && action_name == 'monthly_checklist' ? 'bg-purple-900 dark:bg-purple-800 border-purple-400 dark:border-purple-300 text-purple-100 dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:bg-slate-700 dark:hover:bg-purple-900 hover:border-purple-400 dark:hover:border-purple-300 hover:text-white dark:hover:text-purple-200'} block pl-3 pr-4 py-2 border-l-4 text-base font-medium transition-colors duration-200" %>

        <%= link_to "WhatsApp", whatsapp_auth_path, class: "#{controller_name == 'whatsapp' ? 'bg-purple-900 dark:bg-purple-800 border-purple-400 dark:border-purple-300 text-purple-100 dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:bg-slate-700 dark:hover:bg-purple-900 hover:border-purple-400 dark:hover:border-purple-300 hover:text-white dark:hover:text-purple-200'} block pl-3 pr-4 py-2 border-l-4 text-base font-medium transition-colors duration-200" %>

        <%= link_to "Perfil", profile_path, class: "#{controller_name == 'profile' ? 'bg-purple-900 dark:bg-purple-800 border-purple-400 dark:border-purple-300 text-purple-100 dark:text-purple-100' : 'border-transparent text-slate-300 dark:text-slate-400 hover:bg-slate-700 dark:hover:bg-purple-900 hover:border-purple-400 dark:hover:border-purple-300 hover:text-white dark:hover:text-purple-200'} block pl-3 pr-4 py-2 border-l-4 text-base font-medium transition-colors duration-200" %>
      </div>
    </div>
  </nav>
  <% end %>

  <!-- Main content -->
  <main>
    <% if devise_controller? %>
      <!-- Full-screen layout for authentication pages -->
      <%= yield %>
    <% else %>
      <!-- Standard layout for app pages -->
      <div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
        <div id="flash-messages">
          <% if notice %>
            <div class="bg-green-100 dark:bg-green-900 border border-green-400 dark:border-green-800 text-green-700 dark:text-green-200 px-4 py-3 rounded relative mb-4" role="alert">
              <span class="block sm:inline"><%= notice %></span>
            </div>
          <% end %>

          <% if alert %>
            <div class="bg-red-100 dark:bg-red-900 border border-red-400 dark:border-red-800 text-red-700 dark:text-red-200 px-4 py-3 rounded relative mb-4" role="alert">
              <span class="block sm:inline"><%= alert %></span>
            </div>
          <% end %>
        </div>

        <%= yield %>
      </div>
    <% end %>
  </main>

  <!-- Footer (only show on non-auth pages) -->
  <% unless devise_controller? %>
  <footer class="bg-white dark:bg-dark-bg-secondary border-t border-gray-200 dark:border-gray-700 mt-16">
    <div class="max-w-7xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
      <div class="flex flex-col items-center justify-center space-y-4">
        <div class="flex items-center space-x-2">
          <h1 class="text-lg font-bold text-indigo-600 dark:text-dark-accent">BillBuddy</h1>
          <span class="text-gray-500 dark:text-dark-text-secondary">•</span>
          <span class="text-sm text-gray-500 dark:text-dark-text-secondary">Sistema de Gestão de Aulas</span>
        </div>
        <div class="text-xs text-gray-400 dark:text-gray-500">
          © <%= Date.current.year %> BillBuddy. Desenvolvido para professores e profissionais autônomos.
        </div>
      </div>
    </div>
  </footer>
  <% end %>
</div>

<script>
    // Verificar preferência do usuário para tema escuro
    function updateThemeIcon() {
        const themeIcon = document.getElementById('theme-icon');
        if (document.documentElement.classList.contains('dark')) {
            themeIcon.className = 'fas fa-sun';
        } else {
            themeIcon.className = 'fas fa-moon';
        }
    }

    // Verificar preferência do usuário
    if (localStorage.theme === 'dark' ||
        (!('theme' in localStorage) &&
            window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        document.documentElement.classList.add('dark');
    } else {
        document.documentElement.classList.remove('dark');
    }

    updateThemeIcon();

    // Função para alternar o tema
    function toggleTheme() {
        if (document.documentElement.classList.contains('dark')) {
            document.documentElement.classList.remove('dark');
            localStorage.theme = 'light';
        } else {
            document.documentElement.classList.add('dark');
            localStorage.theme = 'dark';
        }
        updateThemeIcon();
    }

    // Hide WhatsApp status tooltip when clicking outside
    document.addEventListener('click', function(event) {
        const whatsappStatus = document.querySelector('[data-controller="whatsapp-status"]');
        if (whatsappStatus && !whatsappStatus.contains(event.target)) {
            const tooltip = whatsappStatus.querySelector('[data-whatsapp-status-target="tooltip"]');
            if (tooltip) {
                tooltip.classList.add('hidden');
            }
        }
    });
</script>
</body>
</html>
