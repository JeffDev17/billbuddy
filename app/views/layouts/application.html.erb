<!DOCTYPE html>
<html>
<head>
  <title>BillBuddy</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
<div class="container mx-auto px-4">
  <header class="py-4 mb-8 border-b">
    <div class="flex justify-between items-center">
      <h1 class="text-2xl font-bold">
        <%= link_to "BillBuddy", root_path, class: "text-blue-600 hover:text-blue-800" %>
      </h1>

      <% if user_signed_in? %>
        <nav class="flex space-x-4">
          <%= link_to "Dashboard", dashboard_path, class: "px-3 py-2 rounded hover:bg-gray-100" %>
          <%= link_to "Clientes", customers_path, class: "px-3 py-2 rounded hover:bg-gray-100" %>
          <%= link_to "Pacotes", service_packages_path, class: "px-3 py-2 rounded hover:bg-gray-100" %>
          <%= link_to "Compromissos", appointments_path, class: "px-3 py-2 rounded hover:bg-gray-100" %>
          <%= link_to "Pagamentos", payments_path, class: "px-3 py-2 rounded hover:bg-gray-100" %>
          <%= button_to "Sair", destroy_user_session_path, method: :delete, class: "px-3 py-2 bg-red-500 text-white rounded hover:bg-red-600" %>
        </nav>
      <% end %>
    </div>
  </header>

  <% if notice %>
    <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
      <%= notice %>
    </div>
  <% end %>

  <% if alert %>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
      <%= alert %>
    </div>
  <% end %>

  <main>
    <%= yield %>
  </main>

  <footer class="mt-12 py-4 border-t text-center text-gray-500">
    <p>&copy; <%= Date.today.year %> BillBuddy - Sistema de Gerenciamento de Cobranças</p>
  </footer>
</div>
</body>
</html>